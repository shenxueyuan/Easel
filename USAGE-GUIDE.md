# Easel 使用教程

> 从零开始，用 Easel 做小红书图文和短视频内容。

---

## 一、Easel 是什么

Easel 是一个 **AI 内容创作工作台**，帮你从热点发现 → 选题策划 → 内容制作 → 平台发布 → 数据复盘，一站式完成社媒内容运营。

### 核心能力（113 个 Skill）

| 层级 | 能力 | 数量 | 代表 Skill |
|---|---|---|---|
| **发现** | 热点追踪、竞品分析、内容空白发现 | 9 | 热点雷达、竞品分析、算法更新追踪 |
| **策划** | 选题评估、内容策略、排期日历、受众画像 | 16 | 选题评估器、内容矩阵、Hook 生成器 |
| **制作** | AI 生图/视频/音乐/配音、卡片设计、视频剪辑 | 51 | 小红书笔记创作、AI 视频、图文卡片、自动短视频 |
| **发布** | 多平台发布、评论互动、质量检查、定时排期 | 20 | 小红书发布、抖音发布、B站投稿、评论回复 |
| **分析** | 数据追踪、爆款复盘、ROI 计算、评论洞察 | 11 | 发布分析、内容复盘、社交表现报告 |
| **通用** | 画像管理、模板库、批量处理、素材管理 | 6 | 账号画像、模板库、产物管理 |

### 支持的平台

| 平台 | 内容创作 | 自动发布 | 评论互动 | 数据分析 |
|---|---|---|---|---|
| **小红书** | ✅ 笔记+卡片+视频 | ✅ 扫码登录后 | ✅ 回复/删除 | ✅ |
| **抖音** | ✅ 短视频 | ✅ 扫码登录后 | — | ✅ |
| **B站** | ✅ 视频+图文 | ✅ 扫码登录后 | — | ✅ |
| **公众号** | ✅ 长文 | ✅ 扫码登录后 | — | — |
| **知乎** | ✅ 文章+回答 | ✅ 扫码登录后 | — | — |
| **快手** | ✅ 短视频 | ✅ 扫码登录后 | — | — |
| **视频号** | ✅ 短视频 | ✅ 扫码登录后 | — | — |

---

## 二、快速上手（3 步开始）

### 第 1 步：启动 Easel

```bash
cd /Users/chenxueyuan/Documents/backup/github_projects/Easel

# 一键启动（Gateway + LLM Adapter + Web UI）
bash start.sh

# 打开浏览器
open http://localhost:7860
```

其他命令：

```bash
bash start.sh stop       # 停止所有服务
bash start.sh restart    # 重启
bash start.sh status     # 查看状态
bash start.sh logs web   # 看 Web UI 日志（可选 gateway/adapter）
```

> 首次部署需要先运行 `bash setup.sh` 安装依赖。之后日常使用只需 `bash start.sh`。

### 第 2 步：配置账号画像（可选但推荐）

首次打开 Web UI 会弹出引导，建议配置：

1. 点击「开始配置」
2. 填写画像名称（如「科技数码达人」）
3. 选择运营平台（小红书/抖音等）
4. 填写内容方向（如「平价数码测评」）
5. 选择调性（轻松幽默/专业严谨等）
6. 点击「生成画像」

> 画像会让 AI 生成的内容更贴合你的风格。也可以跳过，用「通用模式」。

### 第 3 步：开始对话

1. 点击左侧「对话」
2. 在输入框直接描述需求，例如：
   - "帮我写一条小红书种草笔记，主题是保温杯"
   - "把这段文案做成 3 张小红书知识卡片"
   - "生成一个 15 秒的产品展示视频"
   - "分析一下 @某博主 的小红书爆款规律"

AI 会自动调用对应的 Skill 完成任务。

---

## 三、Web UI 页面导览

| 页面 | 功能 | 什么时候用 |
|---|---|---|
| **工作台** | 首页概览：热点、选题、排期、产物、数据 | 每天打开第一页 |
| **对话** | 与 AI 对话，自动编排 Skill 完成任务 | 日常创作主入口 |
| **技能库** | 浏览/搜索 113 个 Skill，查看每个 Skill 的说明 | 想了解某个能力怎么用 |
| **内容库** | 查看所有生成产物（图片/视频/文案），按主题归档 | 找之前生成的内容 |
| **账号** | 管理各平台登录状态，扫码登录 | 发布前必须先登录 |
| **画像** | 管理账号画像，创建/编辑/切换 | 多人设切换时 |
| **热点雷达** | 微博/抖音实时热搜 | 找选题灵感 |
| **选题库** | 收藏和管理选题 | 攒选题 |
| **内容日历** | 排期管理 | 规划发布节奏 |
| **发布中心** | 多平台发布 | 发布内容 |
| **拆爆款** | 爆款内容拆解分析 | 学习竞品 |

---

## 四、典型使用场景

### 场景 1：写一条小红书图文笔记

```
你：帮我写一条小红书种草笔记，主题是一款保温杯，目标读者是上班族女性

AI 会自动：
1. 分析 卖点 × 稀缺性 × 实用性
2. 生成标题（≤20字，带 emoji）
3. 写 2000-4000 字原稿
4. 去 AI 化
5. 生成 3-9 张图文卡片（1080×1440）
6. 配 hashtags
7. 文本结构校验 + 视觉密度审计
```

产物保存在 `outputs/<主题名>/` 目录。

### 场景 2：制作一条短视频

```
你：帮我做一个 15 秒的产品展示短视频，产品是一款无线耳机

AI 会自动：
1. 生成视频脚本（分镜）
2. AI 生图（产品场景图）
3. AI 生成视频（文生视频/图生视频）
4. AI 配音（CosyVoice）
5. AI 生成 BGM
6. 混音（旁白 + BGM）
7. 加字幕
8. 合成成片
```

### 场景 3：发布到小红书

```
前置：先在「账号」页面扫码登录小红书

你：把刚才那条保温杯笔记发布到小红书

AI 会自动：
1. 校验标题长度（≤20全角字）
2. 校验媒体文件（图片/视频）
3. 内容安全扫描
4. dry-run 预检
5. 确认后启动 Playwright 发布
6. 上传媒体 → 输入标题正文 → 添加话题 → 点击发布
7. 校验发布成功
```

> ⚠️ 发布前 AI 会让你确认标题、正文和媒体。默认 dry-run，需确认后才真正发布。

### 场景 4：回复小红书评论

```
你：帮我回复小红书上最新的评论，用亲切日常的语气

AI 会自动：
1. 列出你的笔记
2. 抓取评论（含子评论）
3. 按画像语气逐条生成回复
4. 间隔 4 秒防风控
5. dry-run 预览 → 确认后 --exec 执行
```

### 场景 5：分析爆款规律

```
你：分析一下小红书上"保温杯"相关的爆款笔记

AI 会自动：
1. 搜索相关笔记
2. 拉取互动数据
3. 分析爆款规律（标题/封面/内容结构）
4. 创作者画像
5. 关键词矩阵
6. 机会评分
7. 输出可执行建议
```

---

## 五、账号管理与登录

### 5.1 Easel 系统本身

**Easel Web UI 不需要登录**。打开 http://localhost:7860 即可使用。

> ⚠️ 如果部署到公网，建议在外层（Nginx/反向代理）加 Basic Auth，因为 `/api/*` 没有鉴权保护。

### 5.2 小红书账号登录

**必须扫码登录**，流程：

1. 打开 Web UI → 左侧「账号」页面
2. 找到「小红书」，点击「登录」
3. 弹出二维码弹窗
4. 用小红书 App 扫码
5. 可能需要短信验证码（风控触发时）
6. 登录成功后，状态变为 ✅

**登录态保存在**：`~/.easel-browser-profiles/XiaohongshuProfile`

**登录态有效期**：由小红书平台 Cookie 决定，通常数天到数周。失效后需重新扫码。

### 5.3 其他平台登录

抖音、B站、公众号、知乎、快手、视频号的登录方式同理：扫码 → Cookie 持久化。

### 5.4 自动发布说明

| 问题 | 答案 |
|---|---|
| 能自动发布吗？ | ✅ 能，但**默认 dry-run**，需确认后才真正发布 |
| 发布前要确认吗？ | ✅ 必须确认标题、正文、媒体 |
| 有风控风险吗？ | ⚠️ 有，自动化发布可能被限流/封号 |
| 建议怎么用？ | 先用测试号、小流量、`--headed` 模式跑通 |
| 评论区能自动回复吗？ | ✅ 能，默认 dry-run，间隔 4 秒防风控 |
| 能点赞/收藏/私信吗？ | ❌ 不支持 |

### 5.5 风控风险提示

- **IP 风险**：机房/代理 IP 可能被拦截，建议用家宽 IP
- **频率限制**：评论/发布不宜过快，脚本已内置间隔
- **内容审核**：发布前自动扫描敏感内容（API Key、内部域名等）
- **账号安全**：建议先用测试号验证，不要直接用主号

---

## 六、LLM 模型切换

当前配置：硅基流动 `DeepSeek-V4-Pro`（Agent 大脑）+ 阿里百炼（视频/音乐/配音）。

切换 LLM 只需改 `.env` 中 `OPENAI_MAAS_MODEL` 一行：

```bash
# Agent 大脑首选（当前）
OPENAI_MAAS_MODEL=deepseek-ai/DeepSeek-V4-Pro

# 小红书语感最佳
OPENAI_MAAS_MODEL=zai-org/GLM-5.2

# 性价比之选
OPENAI_MAAS_MODEL=Qwen/Qwen3.5-397B-A17B

# 极致便宜
OPENAI_MAAS_MODEL=deepseek-ai/DeepSeek-V4-Flash
```

改完后运行 `bash setup.sh` 同步到 OpenClaw。

详见 `KEYS-GUIDE.md` 第十四章 LLM 评测结果。

---

## 七、产物文件管理

所有生成的内容自动下载到：

```
Easel/
└── outputs/
    ├── <主题名>/           ← 每个任务一个目录
    │   ├── note.md         ← 文案
    │   ├── card-1.png      ← 卡片图片
    │   ├── card-2.png
    │   ├── video.mp4       ← 视频
    │   ├── audio.mp3       ← 配音
    │   └── bgm.mp3         ← 背景音乐
    └── _login/             ← 登录二维码等系统文件
```

**规则**：
- 必须放在 `outputs/<具体主题>/` 下，不能散落在根目录
- 主题名不能太泛化（不能用 "test"、"output" 等）
- 在 Web UI「内容库」页面可浏览所有产物

---

## 八、常用命令速查

```bash
# 启动
bash start.sh                      # 一键启动 Gateway + Adapter + Web UI
bash start.sh status               # 查看运行状态
bash start.sh stop                 # 停止所有服务
bash start.sh restart              # 重启
open http://localhost:7860         # 打开 Web UI

# 同步配置（改 .env 后）
bash setup.sh                      # 同步到 OpenClaw

# 命令行直接用
easel chat "帮我写一条小红书笔记"    # CLI 对话
easel doctor                       # 检查配置状态

# 图片生成
python3 skills/shared/scripts/ai_image.py text2img \
  --prompt "一只猫" --output outputs/test/cat.png

# 视频生成
python3 skills/shared/scripts/ai_video.py text2video \
  --prompt "一只猫奔跑" --output outputs/test/cat.mp4 --duration 5

# 小红书登录
python3 skills/shared/scripts/xhs_publish.py login

# 小红书发布（dry-run）
python3 skills/shared/scripts/xhs_publish.py publish \
  --title "标题" --body "正文" --images img1.jpg

# 小红书发布（真正执行）
python3 skills/shared/scripts/xhs_publish.py publish \
  --title "标题" --body "正文" --images img1.jpg --exec
```

---

## 九、常见问题

### Q: 网关离线怎么办？
A: 运行 `bash gateway.sh` 启动 Gateway，确认端口 18789 和 7860 没被占用。

### Q: AI 不回复怎么办？
A: 检查 `.env` 中 API Key 是否正确，运行 `easel doctor` 诊断。

### Q: 视频生成很慢？
A: 正常，5 秒视频约需 1-3 分钟。任务提交后会轮询直到完成。

### Q: 小红书登录二维码过期？
A: 重新点击「登录」按钮，会生成新的二维码。

### Q: 发布失败提示 IP 风险？
A: 小红书对机房 IP 敏感，建议用家宽网络，或在干净网络登录后拷贝 Profile 目录。

### Q: 如何切换不同 AI 模型？
A: 改 `.env` 中 `OPENAI_MAAS_MODEL`，然后 `bash setup.sh`。详见 `KEYS-GUIDE.md`。
